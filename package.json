{
  "name": "property-panel",
  "version": "0.1.0",
  "private": true,
  "description": "PropertyPanel BTL and property portfolio prototype",
  "scripts": {
    "build": "node scripts/build-config.js",
    "dev": "npx serve . -l 4173",
    "check": "node --check app.js"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
